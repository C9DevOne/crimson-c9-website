# CrimsonC9 — Gemini Context

**Read [`AGENTS.md`](./AGENTS.md).** It is the single entry point for every AI tool on this project, and it points at the real documentation in `/docs/`.

Nothing project-specific lives in this file on purpose. Brand facts, design tokens, stack details, and conventions previously duplicated here had already drifted out of sync with the source docs — which is the failure mode this indirection exists to prevent.
